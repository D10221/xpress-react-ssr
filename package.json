{
  "name": "xpress-react-ssr",
  "version": "0.0.1",
  "private": true,
  "description": "express react ssr",
  "main": "index.js",
  "scripts": {
    "start": "cd packages/xpresso && npm start",
    "build": "lerna --no-sort run build",
    "clean-modules": "rimraf **/node_modules/* && rm yarn.lock && yarn",
    "test": "lerna --no-sort run test",
    "set-version": "./scripts/set-version.js"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "workspaces": [
    "packages/tiny-log",
    "packages/tiny-auth",
    "packages/tiny-auth-redux",
    "packages/local-users",
    "packages/local-crypto",
    "packages/local-users-sql",
    "packages/local-auth",
    "packages/dev-middleware",    
    "packages/react-ssr"    
  ],
  "dependencies": {
    "lerna": "2.11.0",
    "rimraf": "^2.6.2"
  }
}